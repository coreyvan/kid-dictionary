# Kid Dictionary

## Init
This service requires Taskfile to be installed. Please refer to [Taskfile documentation](https://taskfile.dev/docs/installation) for installation instructions.

Once installed run:
```shell
task init
```