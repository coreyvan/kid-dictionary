# Contracts: Local Containerized Development Environment

This feature is infrastructure-focused and does not introduce or modify API contracts.

The existing Connect-Go API contracts in `proto/` remain unchanged.
